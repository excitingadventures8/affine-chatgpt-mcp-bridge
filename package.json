{
  "name": "affine-chatgpt-mcp-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only AFFiNE Cloud MCP bridge for ChatGPT using Cloudflare Workers and GitHub OAuth",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "type-check": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.8.1",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "0.17.4",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260914.1",
    "typescript": "6.0.3",
    "wrangler": "4.131.1"
  },
  "engines": {
    "node": ">=20"
  }
}
